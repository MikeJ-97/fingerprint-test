{
  "name": "fpclone-sim",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "node --env-file=.env verify/smoke.mjs",
    "verify": "node --env-file=.env verify/cli.mjs",
    "bots": "node --env-file=.env attacker/bot.mjs",
    "forge": "node --env-file=.env attacker/forge.mjs",
    "test:desktop": "playwright test --config desktop/playwright.config.ts",
    "test:policy": "node --test verify/policy.test.mjs",
    "serve": "node server.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}