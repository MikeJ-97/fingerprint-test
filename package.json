{
  "name": "fpclone-sim",
  "private": true,
  "type": "module",
  "scripts": {
    "verify": "node --env-file=.env verify/cli.mjs",
    "smoke": "node --env-file=.env verify/smoke.mjs",
    "test:desktop": "playwright test --config desktop/playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
